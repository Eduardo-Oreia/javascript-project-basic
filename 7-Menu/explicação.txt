1- crio uma classe com array, com todas as info da comida ex: nome, preço, categoria, id etc...

2- pego a classe section-center do html, e coloco em uma variavel (section-center é pai das info).

3- uso window com addEventListener com  a função "DOMContentLoaded" para cada vez que abrir a janela chamar as funçoes.

4- crio uma função displayMenuItems(menuItems)que recebe um parametro.

 - pego o parametro e uso a função map(), na função map eu uso o comeback que recebe um parametro (item).

 - eu do return com `` e coloco as tags do html com as info da comida.

 - guardo em uma variavel *displayMenu*.

 - pego a var *displayMenu* mudo o valor dela com *displayMenu.join()* uso a função join().

 - pego a var *sectionCenter* uso o innerHTML passando a var *displayMenu*

6- passo a função displayMenuItems() para a window, para quando abrir a janela chamar as funçoes


==============================
*****FILTRANDO AS COMIDAS*****
==============================

7- crio uma variavel *filterButton* para a classe dos buttons filter-btn, nesse caso estou usando o querySelectorAll para pegar todos os buttons.

8- pego a variavel *filterButton* e uso a função forEach() para pegar cada button, nela uso o callback com um parametro (btn).

 - pego o parametro btn e uso o addEL, com a função de click e com um parametro (e)

 - crio uma variavel *category* e passo o parametro.currentTarget.dataset.id para pegar certinho os nomes de cada button

 - crio uma variavel *menuCategory* e passo o nome da minha classe com array, nela uso o .filter com uma função e um parametro (menuItem).

 -uso um if se o menuItem.category for === category return o menuItem.

 -fora da função filter, uso um if e else, if se o category for igual === nome "all" chama a função displayMenuItems(menu) que é a lista toda, else displayMenuItems(menuCategory) que é só refeição certa para o momento

 
================================
*****ADD BUTTON E CATEGORIA*****
================================

9- crio uma função com um parametro
 
 -pego o parametro e passo o valor para,
 *menu* nome da array de itens e uso a função .reduce recebendo dois parametros uma função com callback e um filtro ["all"].

 -no callback recebo dois parametro (values, item)

 -
